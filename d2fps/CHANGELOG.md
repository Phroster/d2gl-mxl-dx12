# Changelog

## 1.0

- Simple player guide: back up and replace the existing `d2fps.dll`, then launch normally.
- Actual Median XL startup confirmed the fork loaded, activated its timing correction and rechecked the client clocks after menu initialization.
- Downloads include the DLL, a ZIP with instructions, matching source and checksums.
- Client-code conflicts are checked by instruction content, independent of module filenames.
- File version and startup label identify this build as 1.0.
- Timing and graphics calculations are unchanged by the repository cleanup. The rebuilt DLL has a new checksum.
